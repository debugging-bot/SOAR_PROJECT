# Python 3.11 required. Python 3.13+ has no mediapipe wheels yet.
# Install:  pip install -r requirements.txt
# Versions are pinned so all 5 members share the same environment.
# A tensorflow version mismatch means you cannot open each other's model files.

numpy>=1.26,<3
opencv-python>=4.9
mediapipe>=0.10.14
tensorflow>=2.16,<2.21
scikit-learn>=1.4
matplotlib>=3.8
pillow>=10.0

fastapi>=0.110
uvicorn>=0.29
websockets>=12.0
